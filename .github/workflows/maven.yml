name: Appium Tests

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    env:
      ANDROID_EMULATOR_WAIT_TIME_BEFORE_KILL: 40 # Increased wait time for emulator shutdown

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install Appium
        run: npm install -g appium

      - name: Start Appium Server
        run: appium --log appium.log &

      - name: Set up Android Emulator
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 30
          target: google_apis
          arch: x86_64
          profile: Nexus 6
          avd-name: test-emulator
          disable-animations: true
          script: echo "Emulator started"

      - name: Wait for emulator to start
        run: adb wait-for-device shell 'while [[ -z $(getprop sys.boot_completed) ]]; do sleep 1; done;'

      - name: Verify emulator is running
        run: adb devices

      - name: Install Maven dependencies
        run: mvn clean install

      - name: Run Appium tests
        run: mvn test

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results
          path: target/surefire-reports/

      - name: Clean up Android Emulator
        run: |
          echo "Shutting down emulator..."
          adb -s emulator-5554 emu kill || echo "Emulator shutdown failed, force killing it."
